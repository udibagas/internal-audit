import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const AuditDetail: React.FC = () => {
  return (
    <div>
      <Title level={2}>Audit Detail</Title>
      <div>Audit detail coming soon...</div>
    </div>
  );
};

export default AuditDetail;
